const Flower = flower => {
    return `
        <section class="flower__name"
            <header>
                <h1>${flower.commonName}</h1>
            </header>
        </section>
        `
}

export default Flower;